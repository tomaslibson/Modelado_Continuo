from scipy.integrate import solve_ivp
import numpy as np
import matplotlib.pyplot as plt

##menores

# alpha = 1

# def system(theta, Y):
#     u, y = Y
#     return [y, -u + 1/alpha]

# thetas = np.linspace(0, 6*np.pi, 2000)
# epsilons = [0.2, 0.6, 0.9, 1.0, 1.1,  1.5, 2]

# for eps in epsilons:
    
#     u0 = (1 + eps)/alpha
#     y0 = 0
    
#     sol = solve_ivp(system, 
#                     [0, 2*np.pi], 
#                     [u0, y0], 
#                     t_eval=np.linspace(0 , 2*np.pi, 2000))
    
    
#     u = sol.y[0]
#     theta = sol.t
    
#     r = 1/u
#     x = r * np.cos(theta)
#     y_cart = r * np.sin(theta)
    
#     # nueva figura para cada ε
#     plt.figure(figsize=(5,5))
    
#     plt.plot(x, y_cart)
#     plt.scatter(0, 0, color='orange')
    
#     plt.axis('equal')
#     plt.title(f"Órbita para ε = {eps}")
#     plt.xlabel("x")
#     plt.ylabel("y")
#     plt.grid()
    
#     plt.show()

#     ##mayores

# alpha = 1

# def system(theta, Y):
#     u, y = Y
#     return [y, -u + 1/alpha]

# epsilons = [1, 1.1, 1.5, 2]

# for eps in epsilons:
    
#     theta_s = np.arccos(-1/eps)
    
#     theta1 = np.linspace(0, theta_s - 0.01, 1000)
#     theta2 = np.linspace(theta_s + 0.01, 2*np.pi - 0.01, 1000)
    
#     plt.figure(figsize=(5,5))
    
#     for theta in [theta1, theta2]:
        
#         u0 = (1 + eps)/alpha
#         y0 = 0
        
#         sol = solve_ivp(system,
#                         [theta[0], theta[-1]],
#                         [u0, y0],
#                         t_eval=theta)
        
#         u = sol.y[0]
#         r = 1/u
        
#         # FILTRO: nos quedamos con la zona física relevante
#         mask = (r > 0) & (r < 10)
        
#         x = r[mask] * np.cos(theta[mask])
#         y_cart = r[mask] * np.sin(theta[mask])
        
        
#         plt.plot(x, y_cart)
    
#     plt.scatter(0, 0, color='orange', label='Sol')
    
#     # 🔍 ZOOM
#     plt.xlim(-5, 5)
#     plt.ylim(-5, 5)
    
#     plt.axis('equal')
#     plt.title(f"Órbita abierta (zoom) ε = {eps}")
#     plt.legend()
#     plt.grid()
    
#     plt.show()